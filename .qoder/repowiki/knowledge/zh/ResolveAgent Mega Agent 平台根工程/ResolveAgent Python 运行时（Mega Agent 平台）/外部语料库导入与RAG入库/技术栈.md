Python asyncio + dataclasses；使用 PyYAML 加载 corpus profile；通过 `subprocess` 调用 `git clone --depth 1` 进行浅克隆；Markdown 分块依赖 `resolveagent.rag.ingest.chunker.TextChunker`，结构化结果经 `resolveagent.rag.dual_writer.DualWriteRAGPipeline` 双写入库。