Python 标准库 `ast` 用于 Python 源码解析；其他语言采用正则启发式解析；可选依赖 `tree-sitter`（由 `parsers/treesitter_parser.py` 提供）；LLM 通过 `resolveagent.llm.provider.ChatMessage` 注入；RAG 通过外部 `rag_pipeline.query` 注入；结果持久化通过可插拔 `call_graph_client` 对接 Go 平台。