通过 `python -m resolveagent.docsync.cli` 或直接 `main()` 运行，默认读取 `docs/i18n/sync-config.yaml`；支持子命令 `sync`（一次性同步）、`watch`（轮询持续同步+校对）、`proofread`（生成校对项）、`glossary list/add`（术语管理）、`review list/resolve`（查看/标记已解决）。配置文件需声明 `defaults`（含 glossary/memory/state/review 路径与 translator.command）和若干 `pairs`。