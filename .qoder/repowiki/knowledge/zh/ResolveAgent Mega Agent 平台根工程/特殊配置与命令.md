顶层 `make help` 列出全部目标；常用工作流：`make setup-dev` 初始化环境，`make build` 一次性构建 Go/Python/Web，`make test` 运行全量测试，`make lint` 执行 golangci-lint/ruff/mypy/buf 联合检查，`make docker` 构建三个镜像，`make compose-up` 启动完整栈，`make helm-install` 部署到集群。