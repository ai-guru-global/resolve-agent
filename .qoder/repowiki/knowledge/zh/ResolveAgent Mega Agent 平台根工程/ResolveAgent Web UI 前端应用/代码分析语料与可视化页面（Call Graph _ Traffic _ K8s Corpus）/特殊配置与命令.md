开发模式下通过设置 `window.localStorage['resolveagent:mock:code-analysis:<tabKey>']`（如 `call-graphs`、`traffic-analysis`）为 `empty`/`error`/`invalid` 来切换 mock 场景；页面顶部通过 `DEV_CODE_ANALYSIS_MOCKS_ENABLED` 开关显示 Mock Banner。