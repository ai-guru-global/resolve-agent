- 所有实体存储均实现 CRUDStore[T] 接口的 Create/GetByID/List/Update/Delete 五个方法，List 统一返回 (entities, total, error) 三元组以支持分页。
- 实体结构体通过嵌入 Timestamped、Versioned、SoftDeletable 混入类型获得创建时间、乐观锁版本号和软删除字段，由存储层自动维护。
- 查询参数统一使用 QueryOptions 结构传递 Offset/Limit/OrderBy/Filters/Search，默认值通过 DefaultQueryOptions() 提供。