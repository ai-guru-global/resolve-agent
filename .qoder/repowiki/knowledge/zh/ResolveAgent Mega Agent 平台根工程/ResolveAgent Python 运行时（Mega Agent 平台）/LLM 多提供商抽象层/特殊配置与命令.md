运行前需设置相应环境变量：直连模式设 `KIMI_API_KEY`/`OPENAI_API_KEY`/`DASHSCOPE_API_KEY` 及 `LLM_BASE_URL`；网关模式设 `HIGRESS_GATEWAY_URL` 与 `RESOLVEAGENT_API_KEY`，并通过 `RESOLVEAGENT_LLM_DIRECT`、`RESOLVEAGENT_GATEWAY_ENABLED` 切换直连/网关路由。