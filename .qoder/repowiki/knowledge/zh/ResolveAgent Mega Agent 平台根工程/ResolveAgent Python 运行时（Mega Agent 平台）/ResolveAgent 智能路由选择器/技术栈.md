Pydantic `BaseModel` 用于 `RouteDecision`/`IntentClassification` 等数据模型；`typing.Protocol` + `@runtime_checkable` 定义结构化协议；`collections.OrderedDict` + `threading.Lock` 实现线程安全的 TTL-LRU 缓存；通过 `resolveagent.resilience.CircuitBreaker` 提供熔断；Hook 体系来自 `resolveagent.hooks`。