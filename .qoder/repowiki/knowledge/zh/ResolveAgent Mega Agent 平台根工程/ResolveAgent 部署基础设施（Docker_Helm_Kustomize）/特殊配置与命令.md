使用 `./deploy/docker/deploy.sh` 一键部署：默认启动生产栈，`dev` 叠加开发模式，`deps` 仅拉起 postgres/redis/nats，`build` 仅构建镜像，`down/logs/status/clean` 管理生命周期；首次运行会自动从 `.env.example` 复制生成 `.env`，必须设置 `RESOLVEAGENT_DATABASE_PASSWORD` 等必填变量。