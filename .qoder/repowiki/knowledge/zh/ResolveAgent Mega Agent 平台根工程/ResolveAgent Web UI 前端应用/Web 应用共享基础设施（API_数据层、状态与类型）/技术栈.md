React + TypeScript；数据请求使用 `@tanstack/react-query`（staleTime=30s, retry=1）；路由使用 `react-router-dom`；全局状态使用 `zustand` + `zustand/middleware/persist`；API 通过原生 `fetch` 访问 `/api/v1`；构建期通过 `import.meta.env.DEV`、`VITE_ENABLE_MOCK`、`VITE_CODE_ANALYSIS_FORCE_REAL` 控制 mock 行为。