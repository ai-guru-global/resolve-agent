本模块以 store.go 中的 Store 生命周期接口（Health/Close）和 patterns.go 中的泛型 CRUDStore[T] + QueryOptions + Timestamped/Versioned/SoftDeletable 混入类型作为跨后端契约；postgres/ 子包基于该契约实现领域实体的关系型持久化，redis/ 子包提供键值缓存后端，memory_store.go 提供进程内内存实现。上层业务通过统一的 Store/CRUDStore 接口调用，无需感知底层是 Postgres、Redis 还是 Memory，从而实现可插拔的持久化策略。