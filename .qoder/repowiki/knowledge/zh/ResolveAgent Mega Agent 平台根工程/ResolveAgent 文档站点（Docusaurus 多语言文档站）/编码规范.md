- 文档内容按主题分目录组织（architecture、user-guide、dev-guide、api、ops、adr 等），并在 sidebars.ts 中显式声明对应 docId 以纳入导航。
- 站点配置集中在 docusaurus.config.ts，通过 i18n.localeConfigs 定义 zh/en 两种语言的显示标签，并以 defaultLocale 指定中文为首发语言。
- Mermaid 图表以 `.mmd` 文件形式存放在 docs/architecture 等目录下，并通过 theme-mermaid 主题在文档中直接引用渲染。
- 所有外部链接统一指向 GitHub 仓库 ai-guru-global/resolve-agent，editUrl 与 navbar 中的 GitHub 入口保持一致。