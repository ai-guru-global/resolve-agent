启动前需根据环境修改 `configs/runtime.yaml` 中的 server.port、store.platform_url 以及 `configs/resolveagent.yaml` 中的 database/redis/nats/gateway 地址；敏感项（如数据库密码、JWT secret）通过环境变量 `RESOLVEAGENT_DATABASE_PASSWORD`、`RESOLVEAGENT_GATEWAY_AUTH_JWT_SECRET` 注入而非明文写入 YAML。