FastAPI + Uvicorn 提供 HTTP/SSE 服务；grpcio 用于 gRPC 服务端（带 ImportError 回退到 aiohttp）；httpx 异步客户端访问 Go Registry；uvicorn 进程式启动；通过环境变量 `RESOLVEAGENT_RUNTIME_HOST/PORT`、`RESOLVEAGENT_PLATFORM_ADDR`、`RESOLVEAGENT_CORS_ORIGINS`、`RESOLVEAGENT_RATE_LIMIT_RPM` 配置运行时。