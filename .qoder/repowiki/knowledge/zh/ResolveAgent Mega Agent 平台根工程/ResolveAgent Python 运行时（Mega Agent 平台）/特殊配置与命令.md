通过 `python -m resolveagent.runtime` 启动 HTTP/gRPC 服务（环境变量 RESOLVEAGENT_RUNTIME_HOST/PORT 控制监听地址），或通过 `resolveagent-docsync`、`kudig-rag-import`、`vectorize-rag-seeds` 三个 entry points 运行离线导入/同步任务；可选依赖 rag 组启用 Milvus/Qdrant 向量库。