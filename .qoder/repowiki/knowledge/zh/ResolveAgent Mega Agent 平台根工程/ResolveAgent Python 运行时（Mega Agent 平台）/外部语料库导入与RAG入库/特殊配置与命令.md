默认缓存目录为 `~/.resolveagent/corpus-cache`，要求系统 PATH 中存在 `git`；可通过 `CorpusImportRequest.source` 传入 Git URL 或本地路径，`force_clone=True` 强制重新拉取；profile 文件位于仓库的 `corpus-config/profiles/<profile_name>.yaml`，默认使用 `rag-sre-profile`。