Dify 插件需先 `pip install dify-plugin`，设置 `RESOLVEAGENT_ENDPOINT` 与 `RESOLVEAGENT_API_KEY` 后在 `integrations/dify/resolveagent-dify` 目录下执行 `dify-plugin package` 生成 `.difypkg` 上传至 Dify；单元测试位于上层 `python` 目录，使用 `uv run pytest tests/test_dify_plugin.py -v` 运行。