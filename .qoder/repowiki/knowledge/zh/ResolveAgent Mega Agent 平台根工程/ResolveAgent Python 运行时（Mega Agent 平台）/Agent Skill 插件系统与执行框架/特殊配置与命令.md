场景型 skill 的 manifest 必须设置 `skill_type: scenario` 且附带 `scenario` 配置块（含 `domain`、`troubleshooting_flow`），否则 `SkillManifest` 构造会抛出 `ValueError`；沙箱执行依赖宿主 Python/Node/Bash 解释器及可选的 `duckduckgo-search`、`beautifulsoup4` 库。