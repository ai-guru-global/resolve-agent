# =============================================================================
# ResolveAgent CI Pipeline — Loop Engineering Continuous Integration Cycle
# =============================================================================
# commit -> lint -> test -> build -> e2e -> docker -> quality-gate
# Each stage gates the next; failures emit feedback signals for rapid iteration.
# =============================================================================

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.25"
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "20"

jobs:
  # ===========================================================================
  # Stage 0: Security — secret leakage scan (full history)
  # ===========================================================================
  security-secrets:
    name: Secret Scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ===========================================================================
  # Stage 1: Lint Gate
  # ===========================================================================
  lint-go:
    name: Lint Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          # v1.64 pinned: the repo config is v1-schema, which golangci-lint v2.x rejects.
          # install-mode: goinstall so the binary is built by CI's Go 1.25 — the prebuilt
          # v1.64 release binary is go1.24-built and refuses go.mod's go 1.25.0.
          version: v1.64
          install-mode: goinstall

  lint-python:
    name: Lint Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@v3
      - name: Install dependencies
        working-directory: python
        run: uv sync
      - name: Ruff check
        working-directory: python
        run: uv run ruff check src/ tests/
      - name: Ruff format check
        working-directory: python
        run: uv run ruff format --check src/ tests/

  lint-web:
    name: Lint Web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile
      - name: ESLint
        working-directory: web
        run: pnpm lint

  # ===========================================================================
  # Stage 2: Unit Tests
  # ===========================================================================
  test-go:
    name: Test Go
    runs-on: ubuntu-latest
    needs: [lint-go]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Run Go tests
        run: go test -race -coverprofile=coverage-go.out -covermode=atomic ./...
      - name: Upload Go coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-go
          path: coverage-go.out

  test-python:
    name: Test Python
    runs-on: ubuntu-latest
    needs: [lint-python]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@v3
      - name: Install dependencies
        working-directory: python
        run: uv sync
      - name: Run Python tests
        working-directory: python
        run: uv run pytest tests/ -v --cov=resolveagent --cov-report=xml
      - name: Upload Python coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-python
          path: python/coverage.xml

  test-web:
    name: Test Web
    runs-on: ubuntu-latest
    needs: [lint-web]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - name: Install dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile
      - name: Type-check & build
        working-directory: web
        run: pnpm build
      - name: Run Web tests
        working-directory: web
        run: pnpm test

  # NOTE: test-mobile deliberately has no `needs` — it runs decoupled from the
  # Go lint→test→build→e2e chain and is only consumed as a gate by docker-build.
  test-mobile:
    name: Test Mobile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: examples/mobile-demo/package-lock.json
      - name: Install dependencies
        working-directory: examples/mobile-demo
        run: npm ci
      - name: Type-check
        working-directory: examples/mobile-demo
        run: npx tsc --noEmit
      - name: Build
        working-directory: examples/mobile-demo
        run: npm run build

  # ===========================================================================
  # Stage 3: Build binaries
  # ===========================================================================
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [test-go, test-python, test-web]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Build Go binaries
        run: |
          mkdir -p bin
          go build -ldflags "-s -w" -o bin/resolveagent-server ./cmd/resolveagent-server
          go build -ldflags "-s -w" -o bin/resolveagent ./cmd/resolveagent-cli
      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: bin/

  # ===========================================================================
  # Stage 4: End-to-End (Go server + Python runtime, in-memory store)
  # ===========================================================================
  e2e:
    name: E2E
    runs-on: ubuntu-latest
    needs: [build]
    env:
      # Strict mode: helper_test.go must FAIL (not skip) when services are
      # unreachable, so a dead server cannot yield a green job with zero tests.
      E2E_STRICT: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@v3
      - name: Install Python runtime dependencies
        working-directory: python
        run: uv sync --python 3.12
      - name: Build Go server
        run: go build -o bin/resolveagent-server ./cmd/resolveagent-server
      # Task 2 已本地验证：e2e 全绿无需 LLM 凭证（LLM 401 时 runtime 仍以 200 流式返回 SSE）。
      # 注意：test/e2e 下全部 4 个测试文件均带 //go:build e2e 标签；不带 -tags e2e 时整个包为空，
      # 所有 e2e 测试会被静默跳过——因此这里的标签是强制的，且 E2E_STRICT 用于防止服务进程已死造成的假绿。
      - name: Start Python runtime
        working-directory: python
        run: |
          RESOLVEAGENT_RUNTIME_PORT=9091 uv run python -m resolveagent.runtime > /tmp/runtime.log 2>&1 &
          echo $! > /tmp/runtime.pid
      - name: Start Go server
        run: |
          ./bin/resolveagent-server > /tmp/server.log 2>&1 &
          echo $! > /tmp/server.pid
      - name: Wait for services
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/healthz > /dev/null && curl -sf http://localhost:9091/health > /dev/null; then
              echo "services ready"; break
            fi
            kill -0 "$(cat /tmp/server.pid)" 2>/dev/null || { echo "Go server process died:"; cat /tmp/server.log; exit 1; }
            kill -0 "$(cat /tmp/runtime.pid)" 2>/dev/null || { echo "Python runtime process died:"; cat /tmp/runtime.log; exit 1; }
            sleep 2
          done
          curl -sf http://localhost:8080/healthz > /dev/null || { echo "Go server failed:"; cat /tmp/server.log; exit 1; }
          curl -sf http://localhost:9091/health > /dev/null || { echo "Python runtime failed:"; cat /tmp/runtime.log; exit 1; }
      - name: Run E2E tests
        run: go test ./test/e2e/... -v -tags e2e -count=1 -timeout 5m
      - name: Stop services
        if: always()
        run: |
          kill $(cat /tmp/server.pid) 2>/dev/null || true
          kill $(cat /tmp/runtime.pid) 2>/dev/null || true

  # ===========================================================================
  # Stage 5: Docker Build
  # ===========================================================================
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [build, test-mobile]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build runtime image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/runtime.Dockerfile
          push: false
          tags: resolve-agent/runtime:ci
      - name: Build platform image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/platform.Dockerfile
          push: false
          tags: resolve-agent/platform:ci
      - name: Build webui image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/webui.Dockerfile
          push: false
          tags: resolve-agent/webui:ci

  # ===========================================================================
  # Stage 6: Quality Gate (feedback loop checkpoint)
  # ===========================================================================
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [e2e, docker-build]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - uses: astral-sh/setup-uv@v3
      - name: Install Python dependencies
        working-directory: python
        run: uv sync --all-groups
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - name: Install Web dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile
      - name: Run quality gate
        run: bash hack/quality-gate.sh
      - name: Feedback loop summary
        run: |
          echo "=== Loop Engineering CI Feedback ==="
          echo "Stage: Quality Gate"
          echo "Status: PASSED"
