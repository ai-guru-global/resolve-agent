运行顺序通常为：先执行 `bash hack/setup-dev.sh` 安装依赖并生成默认配置，再按需运行 `bash hack/generate-proto.sh`、`bash hack/lint.sh`、`bash hack/quality-gate.sh` 以及 `bash hack/coverage-report.sh` 输出覆盖率基线到 test/fixtures/baseline/coverage-baseline.json。