该模块为 Go 包的聚合层，仅包含三个子包：agent（Agent 生命周期管理）、workflow（工作流编排与 FTA 引擎集成）、skill（技能注册与执行协调）。每个子包通过独立的 doc.go 声明职责，顶层 doc.go 汇总说明并标注这些代码属于 internal 包，不对外暴露。当前目录内无实现代码，实际逻辑由各自子包承载，依赖方向由上层调用方引入 platform 下的对应子包。