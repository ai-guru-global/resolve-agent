- 每个子包采用「功能文件 + _test.go」的 Go 标准布局，测试与实现同目录存放。
- 对外能力通过包级初始化函数（如 init()/NewXxx()）暴露，调用方无需感知具体实现。
- 错误处理统一使用 errors 包的 Code 枚举与包装，便于跨层传递结构化错误码。
- 可观测性通过 telemetry 中间件统一注入，handler 不直接调用 logger/metrics/tracer。
- 存储访问一律经由 store.Store 接口，具体后端（postgres/redis/memory）由配置决定，业务代码不感知物理介质。