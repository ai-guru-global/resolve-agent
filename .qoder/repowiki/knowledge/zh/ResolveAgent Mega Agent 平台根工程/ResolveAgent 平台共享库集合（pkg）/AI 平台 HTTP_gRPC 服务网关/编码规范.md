- 每个领域（agent/skill/workflow/rag/memory/hook/solution/callgraph/traffic）独立一个 handlers 文件，按 CRUD 命名 handleList/Get/Create/Update/Delete 等方法
- 所有 REST 端点统一以 /api/v1/{domain}/... 前缀注册，由 router.go 集中挂载
- 处理器通过 Server 字段注入的 registry 接口访问数据，不直接依赖具体存储实现
- 错误响应统一经 response.go 封装为 JSON 格式返回