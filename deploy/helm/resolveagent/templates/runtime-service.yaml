apiVersion: v1
kind: Service
metadata:
  name: {{ include "resolveagent.name" . }}-runtime
  labels:
    {{- include "resolveagent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.runtime.service.port }}
      targetPort: grpc
      name: grpc
  selector:
    {{- include "resolveagent.runtime.selectorLabels" . | nindent 4 }}
