- 所有持久化操作接收 `ctx context.Context` 作为首参，以便支持超时与取消。
- 实体类型通过嵌入 `Timestamped`、`Versioned`、`SoftDeletable` 等混入结构体来复用创建/更新/版本/软删除字段。
- 列表查询统一通过 `QueryOptions` 传入分页（Offset/Limit）、排序（OrderBy 前缀 `-` 表示降序）与过滤条件（Filters map）。
- 接口契约通过包内 mock 实现并在测试中以 `var _ Interface = (*mockImpl)(nil)` 形式进行编译期类型断言。